#include <stdio.h>

/*
 * Create a helper function to find the maximum of two integers
 */
int max(int num1, int num2) {
    if (num1 > num2) {
        return num1;
    } else {
        return num2;
    }
}

/*
 * Complete the function max_of_four.
 */
int max_of_four(int a, int b, int c, int d) {
    // Find the maximum of (a, b) and (c, d), then find the maximum of those two results.
    int max1 = max(a, b);
    int max2 = max(c, d);
    return max(max1, max2);

    /*
     * Alternate approach without a helper 'max' function:
     *
     * int greatest = a;
     * if (b > greatest) {
     *     greatest = b;
     * }
     * if (c > greatest) {
     *     greatest = c;
     * }
     * if (d > greatest) {
     *     greatest = d;
     * }
     * return greatest;
     */
}

int main() {
    int a, b, c, d;
    // The standard input reading is handled by the I/O system, likely using a single scanf call in the background.
    // Assuming the input is provided line by line or space-separated:
    scanf("%d %d %d %d", &a, &b, &c, &d);
    int ans = max_of_four(a, b, c, d);
    printf("%d", ans);
    
    return 0;
}

