#include <stdio.h>
int main() {
    char name[50];
    printf("Enter your name: ");
    // Reads input until it hits a whitespace (space, tab, newline)
    scanf("%s", name); 
    printf("Hello, %s!\n", name);
    return 0;
}
